Simple React frontend that demonstrates:
- Admin Login (username/password)
- Admin Add Challan
- View all challans and counts
- User Signup / Login and view own challans

You must update the backend URL in src/config.js if your backend runs on a different host/port.
